python -m pip install -r /path/to/requirements.txt
python -m streamlit run /path/to/ykk_emergency_response.py
streamlit run ykk_emergency_response.py --server.headless true

Win (CMD)
venv\Scripts\activate
Win (PWSH)
venv\Scripts\activate
ZSH/BASH
source venv/bin/activate
